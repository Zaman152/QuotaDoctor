import ClientContact from "./ClientContact";

export const metadata = {
  title: "Contact Us | QuotaDoctor",
  description: "Get in touch with Dave Thompson to start building your AI workforce and stop missing leads.",
};

export default function ContactPage() {
  return <ClientContact />;
}
